# Notebooks

Suggested notebook sequence:

1. `01_event_dataset.ipynb` - load/resample data and build confirmed box events.
2. `02_streak_analysis.ipynb` - analyze streak duration, continuation probability, and session buckets.
3. `03_turnaround_context.ipynb` - evaluate exhaustion features and turnaround candidate labels.

Keep notebooks exploratory. Move reusable logic into `src/`.
